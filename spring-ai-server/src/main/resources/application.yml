server:
  port: 8080

spring:
  application:
    name: spring-ai-server
  
  ai:
    openai:
      api-key: 
      chat:
        options:
          model: gpt-3.5-turbo
          temperature: 0.7
          max-tokens: 1000

logging:
  level:
    org.springframework.ai: DEBUG
    com.aiflow: DEBUG
